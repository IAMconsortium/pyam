# You can set these variables from the command line.
SPHINXOPTS    =
SPHINXBUILD   = sphinx-build
PAPER         =
BUILDDIR      = build

GHCONFIG      = .gh-config
DOCREPODIR    = build/pyam
DOCREPOURL    = git@github.com:IAMconsortium/pyam.github.io
DOCUPDATENAME = pyam.github.io
